import AppContent from './AppContent'
import AppFooter from './AppFooter'
import AppSidebar from './AppSidebar'

// We removed AppHeader from here because DefaultLayout handles it directly now!
export { AppContent, AppFooter, AppSidebar }